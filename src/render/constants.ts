/** Half-width of the slice the elevation strip shows, in metres either side of the sight line. */
export const SIGHT_BAND = 2.5;

/** Minimum vertical extent the elevation strip is scaled for, in metres. */
export const MIN_ELEVATION_HEIGHT = 5;
